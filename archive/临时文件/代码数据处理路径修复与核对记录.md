# 代码数据处理路径修复与核对记录

## 一、本轮任务范围

本轮围绕 `code/数据处理/` 下脚本的路径修复、输入输出映射确认、重跑结果核对展开。

用户要求：

1. 从 `code/数据处理/` 开始处理。
2. 对每个有输入输出的脚本，找到原始输入输出文件。
3. 运行后的输出结果应尽量与原始输出结果一致。
4. 若遇到错误或无法继续确认的情况，先停下报告。

---

## 二、处理过的脚本

### 1. `10_构建省级能源总量与折标系数.py`

已修复路径：

- 输入：
  - `data/原始数据/各省能源清单CEADS/`
- 输出：
  - `data/外部资料/省级能源折标准煤系数_ceads.csv`
  - `data/中间数据/省级能源energy与es_ceads(暂未启用).csv`

核对结果：

- 重跑后两份输出文件哈希与运行前一致。
- 说明该脚本在当前目录结构下可稳定复现原结果。

---

### 2. `20_夜间灯光指标检查.py`

已修复路径：

- 输入：
  - `data/最终数据/第二阶段_基础.csv`
- 输出：
  - `outputs/数据处理/20_夜间灯光指标检查/`

定位结果：

- 原先第一次核对时，发现输出与仓库中已有结果不一致。
- 进一步定位发现：
  - 该脚本只依赖 `province`、`year`、`lntl`
  - 旧输出对应的面板中，至少有两处 `lntl` 与当前正式面板不同：
    - `上海-2020`
    - `海南-2015`
- 其中：
  - `上海-2020` 已被用户确认是之前数据有误、现已修正
  - `海南-2015` 更像是浮点精度差异

后续处理：

- 按用户要求，直接接受当前正式面板，重跑 `20.py`
- 不再以旧图哈希不一致作为阻塞项

---

### 3. `30_投入产出关系预检.py`

已修复路径：

- 输入：
  - `data/最终数据/第一阶段_基础.csv`
- 输出：
  - `outputs/数据处理/30_投入产出关系预检/09_投入产出关系散点图.png`

核对结果：

- 重跑后输出图哈希与原结果一致。

---

### 4. `40_读取排放比例.py`

脚本性质判断：

- 该脚本是读取排放清单，计算：

$$
es\_proxy=\frac{\text{煤炭相关排放}}{\text{总排放}}
$$

- 它不是正式能源结构变量 `es`，而是近似代理变量脚本。

处理结果：

- 用户确认“如果这是读取排放比例来近似 `es`，就直接归档”。
- 已将其移出正式主线代码目录。

归档路径：

- `archive/旧版文件/手动剔除老数据/40_读取排放比例_能源结构近似.py`

输出核对：

- 重跑后生成的
  - `data/中间数据/排放比例_能源结构近似.csv`
- 与归档原结果
  - `archive/旧版文件/手动剔除老数据/排放比例_能源结构近似_old.csv`
- 哈希完全一致。

---

### 5. `50_样本构建流程缺失检查与变量箱线图.py`

已修复路径：

- 第一阶段输入：
  - `data/最终数据/第一阶段_基础.csv`
- 第二阶段输入：
  - 最终切回 `data/最终数据/第二阶段_基础.csv`
- 输出：
  - `outputs/数据处理/50_样本构建流程缺失检查与变量箱线图/`

核对结果分三部分：

- `图4_样本构建流程图.png`：一致
- `图5_变量缺失热力图.png`：一致
- `图6_核心变量箱线图.png`：文件哈希不一致

但进一步检查发现：

- 两张 `图6` 的图像尺寸一致
- 字体环境正常
- 逐像素比较结果一致
- 差异只出现在 PNG 压缩后的 `IDAT` 数据块

即：

- 图像内容一致
- 文件字节不一致

用户随后要求：

- 将 `50.py` 的第二阶段输入切回正式数据 `data/最终数据/第二阶段_基础.csv`

已按要求切回。

---

## 三、文档与目录同步

本轮同步调整过的文档包括：

1. `docs/约定/中文目录改.md`
2. `docs/约定/项目文件新旧路径对照表.md`

其中包含：

- `40_读取排放比例.py` 已归档
- `code/数据处理/` 主线不再保留以排放比例近似 `es` 的代理脚本

---

## 四、关于 `图6` 哈希不一致的结论

结论如下：

1. 不是 `lntl` 单独导致的问题。
2. 不是绘图内容错误。
3. 当前更准确的判断是：

- 图像像素一致
- PNG 文件压缩编码不一致

即便图完全相同，PNG 在保存时仍可能因为压缩分块或编码路径不同而导致文件哈希不同。

---

## 五、当前状态

当前 `code/数据处理/` 主线可概括为：

- `10_构建省级能源总量与折标系数.py`：已修复并通过核对
- `20_夜间灯光指标检查.py`：已修复并按当前正式面板重跑
- `30_投入产出关系预检.py`：已修复并通过核对
- `40_读取排放比例.py`：已归档，不再作为正式主线脚本
- `50_样本构建流程缺失检查与变量箱线图.py`：已修复，图4与图5一致，图6内容一致但文件哈希不一致

---

## 六、继续检查 `code/效率测算/`

本轮随后按用户要求，继续检查 `code/效率测算/` 下脚本与 `docs/约定/项目文件新旧路径对照表.*`。

涉及脚本：

1. `code/效率测算/10_提取效率测算结果.py`
2. `code/效率测算/20_碳排放效率绘图.py`
3. `code/效率测算/30_效率与灯光排序绘图.py`
4. `code/效率测算/40_生产率分解绘图.py`

### 1. 初始发现的问题

检查时发现：

- `code/效率测算/` 下脚本仍大量沿用旧 `prcd/` 路径
- 当前仓库中已不存在 `code/效率测算/prcd/`
- 也不存在 `code/prcd/`
- 地图、面板、效率结果、分解结果等文件均已迁移到新的 `data/`、`outputs/` 结构

即：

- 这些脚本若按原路径直接运行，会在当前目录结构下失效

### 2. 路径修复

#### `10_提取效率测算结果.py`

修复为：

- 输入目录：
  - `outputs/效率测算/模型输出/分解_超效率SBM Malmquist 指数 -全局参比/`
- 输出文件：
  - `data/中间数据/碳排放效率结果_2015_2022.csv`

源文件定位方式：

- 在 `outputs/效率测算/模型输出/` 下递归查找
  - `*规模报酬可变VRS_0.xlsx`

实际命中源文件为：

- `outputs/效率测算/模型输出/分解_超效率SBM Malmquist 指数 -全局参比/结果_超效率SBM Malmquist 指数 -全局参比_规模报酬可变VRS_0.xlsx`

#### `20_碳排放效率绘图.py`

修复为：

- 输入：
  - `data/最终数据/第二阶段_基础.csv`
  - `data/最终数据/第一阶段_基础.csv`
  - `data/外部资料/中国省级地图.geojson`
- 输出：
  - `outputs/效率测算/10_碳排放效率绘图/`

#### `30_效率与灯光排序绘图.py`

修复为：

- 输入：
  - `data/最终数据/第二阶段_基础.csv`
- 输出：
  - `outputs/效率测算/10_碳排放效率绘图/`

同时修复了原脚本根目录取值错误：

- 原逻辑会把路径错误指向 `code/prcd/`
- 已改为以项目根目录为锚点

#### `40_生产率分解绘图.py`

修复为：

- 输入目录：
  - `outputs/效率测算/模型输出/分解_超效率SBM Malmquist 指数 -全局参比/`
- 输出目录：
  - `outputs/效率测算/20_GM分解绘图/`

并按项目要求将绘图字体统一为：

- `Times New Roman`
- `SimSun`

### 3. 重跑核对结果

重跑结果如下：

- `10_提取效率测算结果.py`：成功
- `20_碳排放效率绘图.py`：成功
- `30_效率与灯光排序绘图.py`：成功
- `40_生产率分解绘图.py`：成功

其中：

- `10.py` 生成的 `data/中间数据/碳排放效率结果_2015_2022.csv`
- 与 `data/最终数据/第二阶段_基础.csv` 中 `eff` 列逐行一致

核对结论：

1. 行数一致
2. `province-year` 组合一致
3. `eff` 数值完全一致
4. 最大绝对差为 `0`

---

## 七、`outputs/效率测算/模型输出/` 目录整理

用户随后提出两点确认：

1. `第一阶段效率测算原始结果/` 这一层是否多余
2. `效率测算结果.xlsx` 与 `结果_超效率SBM Malmquist 指数 -全局参比_规模报酬可变VRS_0.xlsx` 内容是否相同

### 1. Excel 内容核对

核对结果：

- 两个文件都只有 `Sheet1`
- 维度均为 `210 x 9`
- 列名一致
- `DataFrame.equals(...) == True`

即：

- `效率测算结果.xlsx`
- 与
- `结果_超效率SBM Malmquist 指数 -全局参比_规模报酬可变VRS_0.xlsx`

内容完全相同。

### 2. 目录调整

按用户要求执行：

#### 删除中间层

原结构：

- `outputs/效率测算/模型输出/第一阶段效率测算原始结果/分解_超效率SBM Malmquist 指数 -全局参比/`

调整后：

- `outputs/效率测算/模型输出/分解_超效率SBM Malmquist 指数 -全局参比/`

即：

- 将 `分解_超效率SBM Malmquist 指数 -全局参比` 整体上提一层
- 删除空的 `第一阶段效率测算原始结果/`

#### 重命名并归档重复整理件

原文件：

- `outputs/效率测算/模型输出/效率测算结果.xlsx`

归档为：

- `archive/旧版文件/效率测算结果_整理版副本.xlsx`

### 3. 脚本同步

因此同步调整了：

1. `code/效率测算/10_提取效率测算结果.py`
2. `code/效率测算/40_生产率分解绘图.py`

并重跑验证通过。

---

## 八、`项目文件新旧路径对照表` 继续核对与修复

随后对：

1. `docs/约定/项目文件新旧路径对照表.md`
2. `docs/约定/项目文件新旧路径对照表.csv`

做了一轮系统存在性检查。

### 1. 初始核对结果

初次程序化核对结果：

- 共检查 `300` 条
- 不匹配 `60` 条

不匹配项主要分为：

1. 已改名但文档未同步
2. 脚本已迁移到新目录
3. 回归分析输出目录编号漂移
4. 单位根检验输出已改中文名
5. `__pycache__` / `.pyc` 等缓存映射仍保留在表中

### 2. 第一轮批量修复

按用户要求，先处理两组：

1. `可直接自动修正`
2. `可忽略删除`

处理内容包括：

- 对话记录文件名同步到当前实际文件
- `memory` 系列文件名同步
- 作图规范文档从 `docs/概念解释/` 改为 `code/通用工具/`
- 效率测算脚本路径同步到 `code/效率测算/`
- `process1/process2/process2_lag/process2_no_eff` 路径同步到当前文件
- `dearun_eff.csv` 同步到 `data/中间数据/碳排放效率结果_2015_2022.csv`
- `matrix01.csv` 同步到 `data/最终数据/省际01邻接矩阵.csv`
- `模型设定检验` 与 `基准面板回归诊断` 编号同步
- `面板单位根检验` 输出文件名同步为中文名
- 删除 `__pycache__`、`.pyc` 相关映射

第一轮后结果：

- 不匹配从 `60` 条下降到 `10` 条

### 3. 第二轮按用户口径处理剩余条目

用户随后明确：

1. `data/排放比例_es近似.csv`：已归档
2. `data/ceads_energy_total_es_2015_2022.csv`：改到 `data/中间数据/省级能源energy与es_ceads(暂未启用).csv`
6. `data/process.xlsx`：已归档
7. `prcd/chapter4_figures.py`：自动修
8. `prcd/eff_lntl_rank_plot.py`：自动修
10. `prcd/process2_es_proxy.csv`：已归档
其余暂不着急修

据此继续调整：

#### 已归档项

- `data/排放比例_es近似.csv`
  - 改为：`archive/旧版文件/手动剔除老数据/排放比例_能源结构近似_old.csv`
- `data/process.xlsx`
  - 改为：`archive/旧版文件/手动剔除老数据/第二阶段_过程数据表_old.xlsx`
- `prcd/process2_es_proxy.csv`
  - 改为：`archive/旧版文件/手动剔除老数据/分析面板_能源结构代理变量版_old.csv`

#### 自动修正项

- `data/ceads_energy_total_es_2015_2022.csv`
  - 改为：`data/中间数据/省级能源energy与es_ceads(暂未启用).csv`
- `prcd/chapter4_figures.py`
  - 改为：`code/数据处理/50_样本构建流程缺失检查与变量箱线图.py`
- `prcd/eff_lntl_rank_plot.py`
  - 改为：`code/效率测算/30_效率与灯光排序绘图.py`

### 4. 当前剩余未处理项

第二轮后再次核对，剩余不匹配仅有 `4` 条：

1. `data/DEA合并资本存量_无全国.csv -> data/最终数据/资本存量合并数据_无全国.csv`
2. `data/es_.csv -> data/中间数据/能源结构_补充版.csv`
3. `data/es.csv -> data/中间数据/能源结构_整理版.csv`
4. `prcd/process1_2in.csv -> data/中间数据/阶段一双投入面板.csv`

用户已明确：

- 这些项暂不着急修

---

## 九、截至当前的整体状态

截至本轮结束：

### 1. `code/数据处理/`

- 已完成主线脚本路径修复与核对
- `40_读取排放比例.py` 已归档
- 其余脚本可在当前目录结构下运行

### 2. `code/效率测算/`

- 4 个脚本已全部修复
- 已全部重跑通过
- 输出目录与输入路径已切到当前仓库实际结构

### 3. `outputs/效率测算/模型输出/`

- 已删除冗余中间层 `第一阶段效率测算原始结果/`
- 重复整理件 `效率测算结果.xlsx` 已归档

### 4. `docs/约定/项目文件新旧路径对照表.*`

- 已完成大部分有效映射修复
- 仅剩 4 条待后续决定
